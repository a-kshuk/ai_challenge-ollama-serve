# 🤖 Ollama с веб-интерфейсом

Простой способ запустить локальный LLM-сервер с веб-интерфейсом на основе [Ollama](https://ollama.com) и [Ollama WebUI](https://github.com/ollama-webui/ollama-webui).

---

## 🚀 Быстрый старт

1. Убедитесь, что у вас установлены:
   - [Docker](https://docs.docker.com/engine/install/)
   - [Docker Compose](https://docs.docker.com/compose/install/)

2. Запустите сервисы:

   ```bash
   docker-compose up -d
   ```

3. Откройте веб-интерфейс в браузере:
   - [http://localhost:3000](http://localhost:3000)

---

## 🧩 Компоненты

`ollama` — сервер для запуска и управления LLM (например, `llama3`, `mistral`, `gemma`).

`ollama-webui` — визуальный интерфейс для общения с моделями.
Данные моделей хранятся в Docker-томе `ollama_data` (сохраняются между перезапусками).

---

## 🧠 Как использовать модели

После запуска вы можете скачать и запустить любую поддерживаемую модель:

```bash
# Войдите в контейнер ollama
docker exec -it ollama ollama run llama3
```

---

## 🔐 Переменные окружения

| Переменная      | Описание                               |
| --------------- | -------------------------------------- |
| OLLAMA_BASE_URL | Адрес Ollama API (внутри сети Docker). |

---

## 🛑 Остановка

```bash
docker-compose down
```

Чтобы удалить данные (включая модели):

```bash
docker-compose down -v
```

## 🔄 Обновление

```bash
docker-compose pull
docker-compose up -d
```

---

## 🛡️ Примечания по безопасности

Не открывайте порт 11434 наружу (docker-compose.yml этого не делает).

Если вы разворачиваете на публичном сервере — используйте ENABLE_AUTH и WEBUI_SECRET_KEY.

Для HTTPS используйте Nginx + Let's Encrypt.

---

## 📚 Ссылки

Ollama: <https://ollama.com>
Ollama WebUI: <https://github.com/ollama-webui/ollama-webui>
Docker Compose: <https://docs.docker.com/compose/>

💡 Совет: Хотите интегрировать с Obsidian, VS Code или другим приложением? Используйте Ollama API: <http://ваш-сервер:11434>

Разработано с ❤️ для локального ИИ
